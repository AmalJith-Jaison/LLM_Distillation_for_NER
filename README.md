the file contains the scripts required to complete an entire distillation procedure (by pytrch and bedrock)

eml_to_aws(training)::
file contains the script to convert your eml files to promots that can be fed to the aws for training, the script extracts, cleans and add the data from eml files to the prompt template,the prompt is added to a jsonl format whch is in specific format given by aws 

